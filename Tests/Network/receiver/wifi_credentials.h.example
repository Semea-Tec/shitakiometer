#pragma once

// Copie este arquivo para "wifi_credentials.h" (mesma pasta) e preencha com
// as credenciais da sua rede. O arquivo "wifi_credentials.h" e ignorado pelo
// git (ver .gitignore) - cada pessoa configura o seu localmente, a senha
// real nunca e commitada.

const char *WIFI_SSID = "sua-rede";
const char *WIFI_PASSWORD = "sua-senha";
